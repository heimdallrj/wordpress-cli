module.exports = {
  add: function(identifier) {
    console.log('install `%s` plugin', identifier);
  },
  
  remove: function(identifier) {
    console.log('remove `%s` plugin', identifier);
  }
};
